---
title: Download Node.js®
layout: download-simple
---

# Download Node.js®

<WithSimplifiedDownload>
  {({ binaries, installers, version }) => (
    <>
      <h2>Node.js® {version} – Binary Downloads</h2>
      <DownloadsTable source={binaries} />
      <h2>Node.js® {version} – Installer Packages</h2>
      <DownloadsTable source={installers} />
    </>
  )}
</WithSimplifiedDownload>

Learn more about [Node.js releases](/about/previous-releases), including the release schedule and LTS status.

<WithSimplifiedDownload>
  {({ version, urls }) => (
    <>
      <p>
        Read the <LinkWithArrow href={urls.changelog}>changelog</LinkWithArrow>{' '}
        or <Link href={urls.blogPost}>blog post</Link> for this version.
      </p>
      <p>
        <LinkWithArrow href={urls.shasum}>Signed SHASUMS</LinkWithArrow> for
        release files. How to{' '}
        <LinkWithArrow href="https://github.com/nodejs/node#verifying-binaries">
          verify
        </LinkWithArrow>{' '}
        signed SHASUMS.
      </p>
      <p>
        Download a signed{' '}
        <LinkWithArrow href={urls.source}>
          Node.js {version} source
        </LinkWithArrow>{' '}
        tarball.
      </p>
    </>
  )}
</WithSimplifiedDownload>

<WithSimplifiedDownload>
  {({ minors }) =>
    minors.length > 0 && (
      <>
        <h2>Other versions</h2>
        {minors.map(({ binaries, installers, version, urls }) => (
          <details key={version}>
            <summary>
              <h3>Node.js® {version}</h3>
            </summary>
            <div>
              <h4>Binary Downloads</h4>
              <DownloadsTable source={binaries} />
              <h4>Installer Packages</h4>
              <DownloadsTable source={installers} />
              <p>
                Read the{' '}
                <LinkWithArrow href={urls.changelog}>changelog</LinkWithArrow>{' '}
                or <Link href={urls.blogPost}>blog post</Link> for this version.
              </p>
              <p>
                Download a signed{' '}
                <LinkWithArrow href={urls.source}>
                  Node.js {version} source
                </LinkWithArrow>{' '}
                tarball.
              </p>
            </div>
          </details>
        ))}
      </>
    )
  }
</WithSimplifiedDownload>
