<!DOCTYPE html>
<html lang="{{site.locale}}"{{#if site.rtl}} dir="rtl"{{/if}}>
{{> html-head }}

<body>
  {{> header }}

  <main id="main">
    <div class="container">

      <article>
        {{{ contents }}}
      </article>

    </div>
  </main>

  {{> footer }}
</body>
</html>
