<!DOCTYPE html>
<html lang="{{site.locale}}">
{{> html-head }}

<body>
  {{> header }}

  <div id="main">
    <div class="container has-side-nav">

      {{> navigation key='foundation'}}

      <article>
        {{{ contents }}}
        {{#if newsletter}}
          {{> mc-signup-form newsletter }}
        {{/if}}
      </article>

    </div>
  </div>

  {{> footer }}
</body>
</html>
