/**
 * Playwright configuration for the default platform: tests run against an
 * externally started server (see `PLAYWRIGHT_BASE_URL`), so there is nothing
 * to add to the shared configuration in `apps/site/playwright.config.mjs`.
 *
 * @type {import('@playwright/test').PlaywrightTestConfig}
 */
export default {};
