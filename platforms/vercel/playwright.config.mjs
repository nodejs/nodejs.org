/**
 * Playwright configuration for the Vercel platform: tests run against a
 * deployed preview (see `.github/workflows/playwright.yml`), so there is
 * nothing to add to the shared configuration.
 *
 * @type {import('@playwright/test').PlaywrightTestConfig}
 */
export default {};
