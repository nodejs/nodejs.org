{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": [],
      "outputs": ["dist/**"]
    },
    "lint:js": {
      "inputs": ["lib/**/*.{js,mjs,ts}"]
    }
  }
}
