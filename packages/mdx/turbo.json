{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "lint:js": {
      "inputs": ["lib/**/*.{js,mjs,ts}"]
    },
    "test:unit": {
      "inputs": ["**/*.{ts,tsx,mjs}"]
    }
  }
}
