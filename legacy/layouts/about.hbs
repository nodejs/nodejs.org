<!DOCTYPE html>
<html lang="{{site.locale}}"{{#if site.rtl}} dir="rtl"{{/if}}>
{{> html-head }}

<body>
  {{> header }}

  <main id="main">
    <div class="container has-side-nav">

      {{> navigation key='about'}}

      <article dir="auto">
        {{{ contents }}}
      </article>

    </div>
  </main>

  {{> footer }}
</body>
</html>
