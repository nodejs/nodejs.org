import EventsImages from '.';
import type { Meta as MetaObj, StoryObj } from '@storybook/react';

type Story = StoryObj<typeof EventsImages>;
type Meta = MetaObj<typeof EventsImages>;

export const Default: Story = {};

export default { component: EventsImages } as Meta;
